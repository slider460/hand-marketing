# Предстартовый аудит hand-marketing.ru (29.06.2026)

Цель: перед переводом домена с Тильды убедиться, что новый билд боевой, без SEO-флагов и тормозов. Эталон — живая Тильда (10 лет), требование — паритет 1:1.

## ✅ Что в порядке (проверено на прод-сервере)
- **60 страниц**, все внутренние ссылки отвечают 200/301, 404 отдаёт 404.
- **sitemap.xml** — все URL резолвятся; добавлен пропущенный `/portfolio/stavropol-stand-vdnh` (теперь 60).
- **canonical** на всех Тильда-страницах — на себя; **robots.txt** боевой (`Allow: /`), стейджинг закрыт по хосту (`X-Robots-Tag: noindex`).
- **Нет mixed-content** (http-ресурсов нет → https безопасен).
- `lang=ru`, favicon, og:image — на месте и резолвятся.
- **Шрифты** Circe: `font-display: swap` (нет «невидимого текста» при загрузке).
- **PHP/формы** работают (`/api/lead.php` → 405 на GET; письма на `info@hand-marketing.ru`).
- **JS легче Тильды**: 239 КБ vs 345 КБ (gzip) на главной.
- Заголовки `<title>` совпадают с 10-летней Тильдой (slug-имена `Event_Riviera` и т.п. — это паритет, не регресс; на Тильде они такие же и без description).

## 🔴 Красные флаги — ИСПРАВЛЕНО / ТРЕБУЕТ РЕШЕНИЯ

1. **[ИСПРАВЛЕНО] React-кейсы канонизировались на главную.** 3 страницы (`samara-stand-vdnh`, `samara-exhibition`, `stavropol-stand-vdnh`) имели `canonical → /` и одинаковый `<title>` → поисковик счёл бы их дублями главной и выкинул из индекса. Прописаны уникальные `title`/`description`/`canonical`/`og`.

2. **[ИСПРАВЛЕНО] Ставрополь отсутствовал в sitemap.** Добавлен.

3. **[ИСПРАВЛЕНО] Вернул веб-аналитику.** Яндекс.Метрика (счётчик **71125393**, тот же, что на Тильде) добавлена на все 60 страниц + 3 React-кейса. Встроена в `scripts/a2/build_v1.py` (для будущих сборок) и пропатчена в готовые файлы (`scripts/a2/add_metrika.py`). Без дублей.

4. **[ИСПРАВЛЕНО] Картинки оптимизированы (WebP, 1:1).**
   - Сгенерировано **630 `.webp`** (`scripts/gen-webp.sh`, q82): **225.6 МБ → 59.5 МБ (−74%, экономия 166 МБ)**.
   - `.htaccess` отдаёт `.webp` браузерам по `Accept: image/webp` (оригиналы `.png/.jpg` — фолбэк, ссылки в HTML не трогаются, визуал 1:1, размеры пиксель-в-пиксель совпадают).
   - Главная по картинкам: ~22.6 МБ → ~6 МБ.
   - ⚠️ Для деплоя `.webp` (≈60 МБ) нужно закоммитить и запушить (CI зальёт их в `mirror/`).
   - Осталось (опц.): удалить мёртвый `mirror/images/team/awards-ceremony.jpg` (15.6 МБ, неиспользуемый React-чанк `About-*`).

## 🟡 Мелочи (опционально)
- 32 страницы имеют общий generic-description. На Тильде у них description нет вовсе — т.е. не регресс. Можно проставить уникальные из данных каталога (`getproductslist_*.json`).
- Slug-заголовки кейсов (`Creative_Patriki`) — паритет с Тильдой; при желании причесать.
- Удалить мёртвый 15.6 МБ `awards-ceremony.jpg` (+ при желании неиспользуемый About-чанк) — экономия деплоя.
