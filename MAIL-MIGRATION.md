# Перенос почты hand-marketing.ru: Яндекс → Reg.ru

Дата плана: 15.07.2026. Делаем утром вместе. Отмечай галочки по ходу.

## Расклад (факт на 15.07.2026)
- **DNS-зона:** nic.ru (ns3/ns4/ns8-l2.nic.ru) — ВСЕ записи правим в панели **nic.ru**.
- **Сайт-хостинг:** Reg.ru, IP `31.31.197.38` — сюда переносим и почту.
- **Почта сейчас:** Яндекс. MX `mx.yandex.net` (TTL **21600** = 6 ч), DKIM яндексовский на `mail._domainkey`, DMARC — нет.
- **SPF сейчас:** `v=spf1 ip4:31.31.197.38 include:_spf.yandex.net ~all`.
- **Ящики:** `anarodetsky@hand-marketing.ru` (⚠️ АРХИВ ~10 лет — двойной бэкап, ничего не удалять в Яндексе до подтверждения!), `info@hand-marketing.ru`.
- **Заявки с форм не теряются** — пишутся в `api/leads.csv` на сервере в любом случае. `lead.php` шлёт через `mail()` — после переезда MX+SPF+DKIM на Reg.ru заработает «нативно», код менять НЕ надо. `?selftest` уже убран (прод отдаёт 405).
- ⚠️ Яндекс.Почта для домена стала платной ~14.07 — проверить, не начались ли ограничения/грейс; это добавляет срочности, но переезд всё равно делаем по порядку (архив — первым).

---

## ФАЗА 0 — Подготовка (сделать ПЕРВЫМ, до всего; можно с вечера)
Цель — чтобы завтрашнее переключение разъехалось за минуты, а не за 6 часов, и был быстрый откат.

- [ ] **0.1 Понизить TTL в nic.ru** у записей `MX` и `TXT` (SPF) до **300** сек. (Сейчас MX=21600.) Дать записям «прожить» новый TTL хотя бы старый интервал перед сменой — потому и делаем заранее.
- [ ] **0.2 Инвентаризация Яндекса:** зайти в оба ящика, записать примерный объём/кол-во писем `anarodetsky@` (для сверки после переноса). Убедиться, что доступ по IMAP включён (Яндекс: Настройки → Почтовые программы → «С сервера imap.yandex.ru по протоколу IMAP» ВКЛ, и разрешить пароли приложений).
- [ ] **0.3 Пароль приложения Яндекса** для imapsync (если включена 2FA): Яндекс ID → Безопасность → Пароли приложений → создать для «почты». Записать.
- [ ] **0.4 Проверить, что тариф хостинга Reg.ru включает почту** для домена (у большинства shared-тарифов Reg.ru — да; домен уже привязан к хостингу, раз сайт работает).

## ФАЗА 1 — Ящики на Reg.ru (утром, ДО смены MX)
- [ ] **1.1** В панели Reg.ru (раздел «Почта» хостинга) создать ящики:
      `anarodetsky@hand-marketing.ru` и `info@hand-marketing.ru`, задать надёжные пароли (записать в менеджер паролей — НЕ в чат).
- [ ] **1.2** Забрать из панели Reg.ru параметры (понадобятся дальше):
      - имя **MX-сервера** Reg.ru (обычно вида `mail.hosting.reg.ru` или конкретный `mxN.hosting.reg.ru` — взять точное значение из панели);
      - **IMAP/SMTP** хост+порты (обычно `mail.hosting.reg.ru`, IMAP 993 SSL, SMTP 465 SSL);
      - **DKIM**: включить подпись DKIM для домена и скопировать сгенерированную запись (селектор + значение `p=...`). У Reg.ru селектор часто НЕ `mail` — записать точный `<селектор>._domainkey`.
      - **SPF include** Reg.ru (обычно `include:_spf.reg.ru` — уточнить в подсказке панели).

## ФАЗА 2 — Перенос архива (ДО смены MX! Критично для anarodetsky@)
Пока MX ещё на Яндексе — переносим накопленное, чтобы ничего не потерять.

- [ ] **2.1 Локальный бэкап** `anarodetsky@` — самое важное. Вариант А: Thunderbird → добавить ящик Яндекса по IMAP → ImportExportTools NG → экспорт всех папок в `.mbox` на диск. Вариант Б: `imapsync` в локальную папку. Держать копию офлайн.
- [ ] **2.2 imapsync Яндекс → Reg.ru** (оба ящика). Пример команды (подставить реальные хосты/пароли; ставится `brew install imapsync` или готовый образ):
      ```
      imapsync \
        --host1 imap.yandex.ru --user1 anarodetsky@hand-marketing.ru --password1 'ЯНДЕКС_APP_PW' --ssl1 \
        --host2 mail.hosting.reg.ru --user2 anarodetsky@hand-marketing.ru --password2 'REGRU_PW' --ssl2 \
        --automap --no-modulesversion
      ```
      Повторить для `info@`. Прогнать 2 раза (второй прогон — синхронизирует «хвост», должен показать 0 новых).
- [ ] **2.3 Сверка:** число писем/папок на Reg.ru совпало с записанным в 0.2. **НИЧЕГО в Яндексе НЕ удалять.**

## ФАЗА 3 — Переключение DNS в nic.ru (когда ящики + архив готовы)
Меняем все 4 записи в одном заходе:

- [ ] **3.1 MX:** удалить `10 mx.yandex.net`, добавить MX Reg.ru из п.1.2 (напр. `10 mail.hosting.reg.ru.`). TTL 300.
- [ ] **3.2 SPF (TXT @):** заменить на Reg.ru-вариант, СОХРАНИВ ip хостинга для форм:
      `v=spf1 ip4:31.31.197.38 include:_spf.reg.ru ~all`  (убрать `include:_spf.yandex.net`; include Reg.ru — из п.1.2).
- [ ] **3.3 DKIM:** добавить запись DKIM Reg.ru из п.1.2 на её селектор `<селектор>._domainkey` (TXT). Старую яндексовскую `mail._domainkey` можно оставить или удалить — она не помешает, если селектор другой.
- [ ] **3.4 DMARC:** добавить TXT `_dmarc.hand-marketing.ru` →
      `v=DMARC1; p=none; rua=mailto:postmaster@hand-marketing.ru; fo=1`  (стартуем с `p=none` — мониторинг, без риска резать письма).

## ФАЗА 4 — Проверка (через ~5–15 мин, TTL 300)
- [ ] **4.1** `dig MX/TXT hand-marketing.ru` и `dig TXT <селектор>._domainkey / _dmarc.` — видим Reg.ru-значения. (Из песочницы DNS по UDP не идёт — проверять с обычной машины или https://dns.google/resolve.)
- [ ] **4.2 mail-tester.com:** отправить туда письмо (а) из обычного клиента и (б) с сайта через форму → цель **10/10**, SPF+DKIM+DMARC = pass.
- [ ] **4.3 Форма сайта end-to-end:** оставить заявку на hand-marketing.ru → письмо пришло в inbox (не спам) + строка в `leads.csv`.
- [ ] **4.4 Входящие:** отправить письмо с внешнего адреса на оба ящика → дошло на Reg.ru.

## ФАЗА 5 — Наблюдение и завершение
- [ ] **5.1** 2–3 дня держать ящики Яндекса как резерв (MX уже на Reg.ru, но копия остаётся).
- [ ] **5.2** Убедиться за эти дни: входящие идут на Reg.ru, архив на месте и открывается, формы доходят.
- [ ] **5.3** Только после этого — отключить/освободить почту в Яндексе.
- [ ] **5.4** (опционально) Вернуть Telegram-уведомления форм: создать `api/config.php` на сервере с `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` (файл в git и в деплой НЕ попадает — исключён). `lead.php` подхватит.
- [ ] **5.5** Через неделю мониторинга DMARC (rua-отчёты) при желании ужесточить до `p=quarantine`.

---
## Что делаю Я, что делаешь ТЫ
- **Ты** (нужны логины/пароли и панели — я к ним не имею и не должен иметь доступа): всё в панелях **nic.ru** (DNS) и **Reg.ru** (ящики, DKIM), пароли ящиков, пароль приложения Яндекса, imapsync.
- **Я**: подскажу точные значения записей под твою панель, соберу/проверю imapsync-команду, прогоню проверки DNS/доставки (dig через DoH, curl формы, чтение `leads.csv`), поправлю `lead.php`/`config.php` если понадобится. Пароли — только через твой менеджер паролей, в чат не пишем.
